---
description: Read the project overview spec before working
alwaysApply: true
---

# Read The Overview First

Always read `specs/overview.md` first, before making code, documentation, or test changes in this repository. Follow its architecture and testing guidance.

Use `make test` for the regular automated test suite. Do not use `uv run pytest` directly for normal validation.
